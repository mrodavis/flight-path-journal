<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/_navbar') %>

<h1>Edit Flight Session</h1>

<form action="/users/<%= userId %>/sessions/<%= session._id %>?_method=PUT" method="POST">
  <label>Date:</label>
  <input type="date" name="date" value="<%= session.date.toISOString().split('T')[0] %>" required><br>

  <label>Duration (minutes):</label>
  <input type="number" name="duration" value="<%= session.duration %>" required><br>

  <label>Focus Area:</label>
  <input type="text" name="focusArea" value="<%= session.focusArea %>" required><br>

  <label>Notes:</label>
  <textarea name="notes"><%= session.notes %></textarea><br>

  <button type="submit">Update Session</button>
</form>

</body>
</html>