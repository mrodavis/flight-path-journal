<nav class="navbar">
  <div class="logo">
    
  </div>
  <div class="links">
    <a href="/">Home</a>
    <% if (user && user._id) { %>
      <a href="/users/<%= user._id %>/sessions">My Sessions🛩️</a>
      <a href="/users/<%= user._id %>/milestones">My Milestones✈️</a>
      <a href="/auth/sign-out">Sign Out</a>
    <% } else { %>
      <a href="/auth/sign-in">Sign In</a>
    <% } %>
  </div>
</nav>
