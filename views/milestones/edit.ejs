<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Milestone</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/_navbar') %>

  <main class="session-form-wrapper">
    <div id="flightCard" class="form-card">
      <h1>✏️ Edit Milestone</h1><br>
      <form action="/users/<%= userId %>/milestones/<%= milestone._id %>?_method=PUT" method="POST" class="flight-form">

        <label for="type">Type:</label>
        <input type="text" name="type" id="type" value="<%= milestone.type %>" required />

        <label for="description">Description:</label>
        <textarea name="description" id="description" rows="4"><%= milestone.description %></textarea>

        <label for="date">Date:</label>
        <input type="date" name="date" id="date" value="<%= milestone.date ? milestone.date.toISOString().split('T')[0] : '' %>" />

        <label for="status">Status:</label>
        <select name="status" id="status">
          <option value="Pending" <%= milestone.status === "Pending" ? "selected" : "" %>>Pending</option>
          <option value="Complete" <%= milestone.status === "Complete" ? "selected" : "" %>>Complete</option>
        </select>

        <div class="cta-container">
          <button type="submit" class="btn cta-btn">Update Milestone</button>
        </div>
      </form>

      <div class="cta-container">
        <a href="/users/<%= user._id %>/milestones" class="btn cta-btn">Back to Milestones</a>
      </div>
    </div>
  </main>
</body>
</html>
