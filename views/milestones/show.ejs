<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= milestone.type %> - Milestone</title>
  <link rel="stylesheet" href="/css/dashboard.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/_navbar') %>

  <main class="session-form-wrapper">
    <div class="session-details-card">
      <h1 style="text-align:center;">🌟 <%= milestone.type %></h1>

      <ul class="session-info-list">
        <li><strong>Status:</strong> <span class="session-badge"><%= milestone.status %></span></li>
        <li><strong>Date:</strong> <%= milestone.date ? milestone.date.toDateString() : 'N/A' %></li>
        <li><strong>Description:</strong> <br> <em><%= milestone.description || 'No description provided.' %></em></li>
      </ul>

      <div class="session-actions">
        <a href="/users/<%= userId %>/milestones/<%= milestone._id %>/edit" class="btn btn-edit">✏️ Edit</a>
        <form action="/users/<%= userId %>/milestones/<%= milestone._id %>?_method=DELETE" method="POST" style="display:inline;">
          <button type="submit" class="btn btn-delete">🗑 Delete</button>
        </form>
      </div>

      <div class="center-text">
        <a href="/users/<%= userId %>/milestones" class="btn-outline">⬅ Back to Milestones</a>
      </div>
    </div>
  </main>
</body>
</html>
