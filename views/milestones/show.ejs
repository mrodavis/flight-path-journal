<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= milestone.type %> - Milestone</title>
  <link rel="stylesheet" href="/css/dashboard.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../partials/_navbar') %>

  <main class="session-form-wrapper">
    <div id="flightCard" class="form-card">
      <h1>🌟 <%= milestone.type %></h1><br>

      <p><strong>Status:</strong> <%= milestone.status %></p>
      <p><strong>Date:</strong> <%= milestone.date ? milestone.date.toDateString() : 'N/A' %></p>
      <p><strong>Description:</strong> <%= milestone.description || 'No description provided.' %></p>
      
      <div class="cta-container">
        <a href="/users/<%= userId %>/milestones/<%= milestone._id %>/edit" class="btn cta-btn">✏️ Edit</a>
        <form action="/users/<%= userId %>/milestones/<%= milestone._id %>?_method=DELETE" method="POST" style="display:inline;">
          <button type="submit" class="btn cta-btn">🗑 Delete</button>
        </form>
      </div>

      <div class="cta-container">
        <a href="/users/<%= userId %>/milestones" class="btn cta-btn">⬅ Back to Milestones</a>
      </div>
    </div>
  </main>
</body>
</html>
